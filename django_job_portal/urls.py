# D:\django-job-portal-master\django_job_portal\urls.py
# This file should now be empty because jobs/urls.py is the ROOT_URLCONF.

from django.urls import path

urlpatterns = [
    # This file should now be empty or contain only very specific patterns
    # that don't conflict with jobs/urls.py being the main URLConf.
]

